# EB-Agent 首页对话助手（统一智能入口）实施方案

> 状态：方案设计稿（未落地，待评审）
> 范围：平台首屏对话入口，覆盖「问答 / 动作驱动 / Agentic RAG」三大能力
> 原则：纯新增 + 小改，不破坏现有 dump / digest / build / 桥接 / 任务系统

---

## 一、定位与目标

把之前讨论的「问询面板浮层」升级为**平台首屏对话入口**：用户进入 EB-Agent 先看到对话页，再按需跳进公告 / 数据 / 舆情三个项目。一个对话框同时满足三件事：

1. **问内容**：跨 Anno / Data / Wechat 问具体数据细节（"132xxx.SH 这只 EB 风险如何""今天舆情抓了哪些关键词"）。
2. **驱动功能**：自然语言触发现有功能（"帮我一键精读""重建一下数据""抓取舆情"）。
3. **Agentic RAG**：回答时自主检索**历史对话记忆**与**三项目实时数据**，多跳调用、带依据生成。（"今天的舆情和昨天有什么区别"）

本质 = 把 bridge 模式里「人搬运 prompt」这一步内化：助手自己调平台 API、自己取数，只在**必须人审**的环节才停下来。

---

## 二、页面形态（在数据窗口之前）

- `web/index.html`：顶部 tab 顺序改为 **对话助手 | 公告 | 数据 | 舆情 | 设置**，默认 `active` 落在「对话助手」。
- 首屏是一整页对话：对话流 + 右侧「快捷指令」芯片（一键精读 / 重建数据 / 抓取舆情 / 校验公告 / 问某债风险）。
- 三个项目页**完全不动**；对话中执行动作后可一键跳转到对应项目看结果。
- 对话区顶部显示当前「会话」入口，可「开启新对话」「查看历史」。

---

## 三、能力总览

| 能力 | 触发示例 | 后端路径 | 是否需人 |
|---|---|---|---|
| 问答（内容级） | "xx 债的风险如何" | datareader + SenseNova | 否 |
| 动作（全自动） | "重建数据""抓取舆情" | 调 h_data_rebuild / h_wechat_crawl | 否 |
| 动作（桥接） | "一键精读" | 调 h_anno_digest + bridge | **是**（回填） |
| Agentic RAG | "上次那只债后来怎样了" | memory + 多源检索 + SenseNova | 否 |

---

## 四、请求流转

```
用户输入 → POST /api/assistant
         → assistant.classify() 意图识别
            ├─ Action（驱动功能）
            │     → 匹配动作模板(参数抽取)
            │     → 回"将要执行"预览卡 + 确认按钮
            │     → 用户确认 → 调对应 h_* → 任务系统跑 → 前端订阅进度(SSE)
            └─ Q&A / 含指代 / 需依据
                  → RetrievalPlanner 规划要查的源
                  → 并发检索: memory / anno / data / wechat / tasks / prompt
                  → (可选)评估不足 → 调整 query 二次检索(多跳, ≤2 轮)
                  → 组装 context → SenseNova 直连生成 → 回答(附引用源)
```

---

## 五、数据读取器（datareader，答内容细节的关键）

新建 `app/core/datareader.py`，读取三项目成品/源（复用平台既有解析能力，不重造）：

| 项目 | 读取源 | 解析方式 |
|---|---|---|
| Anno | `Anno/out/*.xlsx`（成品表）、`Anno/input.json`、`Anno/_txt_dump/*.txt` | openpyxl（依赖解释器已装）/ 直接读 txt |
| Data | `Data/可交换债数据.xlsx`（15 列总表） | openpyxl |
| Wechat | `Wechat/*.docx`（舆情） | python-docx（依赖解释器已装） |

- **索引**：首次访问 / 文件变更时建轻量索引——按 **EB 代码 / 名称 / 公告日期** 建 dict，定位到行。问"xx 债"直接取对应行，避免每次全量扫文件。
- **截断**：上下文超限时只带命中行 + meta，不整表灌入。

---

## 六、动作执行器（意图 → 现有 API 映射，端点已核实）

| 用户说法 | 调 API | 模式 |
|---|---|---|
| "一键精读 / 精读这些公告" | `POST /api/anno/digest` | **桥接**（触发任务 + 生成 bridge prompt，提示"请在桥接面板提交结果"） |
| "生成整理表 / 出表" | `POST /api/anno/build` | 全自动 |
| "转储公告 / 抽正文" | `POST /api/anno/dump` | 全自动 |
| "校验公告" | `POST /api/anno/check` | 全自动 |
| "重建数据 / 数据整合" | `POST /api/data/rebuild` | 全自动 |
| "下载上证/深证数据" | `POST /api/data/download-yz` `/sz` | 全自动 |
| "抓取舆情 / 爬公众号" | `POST /api/wechat/crawl` | 全自动 |
| "最近任务怎样 / 为什么失败" | `GET /api/tasks/<tid>` + `/log` | 全自动（读持久化日志） |

> 说明：`digest` 精读是 bridge 阻塞模型（需人在 WorkBuddy 回填），助手无法替人搬运——走「触发任务 + 暴露 bridge 待办」，与现有 `analyze` 指令逻辑一致；其余动作可**全自动**跑完并回传进度。UI 上对用户透明标注「需桥接 / 全自动」徽标。

---

## 七、Agentic RAG（历史记忆 + 实时检索调用）★ 本方案核心新增

### 7.1 概念
- **RAG**：回答前先检索外部知识增强生成。
- **Agentic**：由 agent 自主决定「是否检索、检索哪个源、检索几次（多跳）」，而非固定拼接 pipeline。
- **两个记忆对象**：
  - 会话内记忆：当前对话多轮上下文（标准 chat 自带）。
  - 跨会话历史记忆：持久化的历史对话，可随时被检索调用（"上次你说过 xx 债…"）。

### 7.2 架构
```
用户问题
  → assistant.classify() 分流
  → 若需知识(Q&A / 含指代 / 需依据):
       planner = RetrievalPlanner(rules 或 LLM)
         → 规划本轮要查的源: [memory, anno:<code>, data, wechat, tasks, prompt]
         → 并发检索各源 → 片段
         → (可选)评估不足 → 调整 query 二次检索(多跳)
         → 组装 context → SenseNova 生成
  → 若动作: 走 execute_action (见六)
```

### 7.3 记忆层（memory.py，新建）
- **存储**：`runtime/memory/<session_id>.jsonl`，每行 `{ts, role:user|assistant|tool, text, refs}`。
- **会话 ID**：前端 localStorage 持有；可「开启新对话」。历史会话列表从 `runtime/memory/` 读取。
- **跨会话检索**：`search_memory(query, top_k)` 对历史 jsonl 做时间倒序 + 关键词匹配，返回相关片段（**不限于当前会话**）。
- **记忆清理**：设置页「清除对话记忆」→ rename-aside 归档（沿用平台 rename 护栏，避开批量删除守卫）。
- **不存**：密钥、文件正文全文（只存对话摘要 / 引用路径）。

### 7.4 检索层（retrieval.py，新建，多源）

| 源 | 检索器 | 返回 |
|---|---|---|
| memory | 历史对话 jsonl | 相关对话片段（跨会话） |
| anno | `datareader.read_anno(code/name/date)` | 成品表命中行 / 转储正文 |
| data | `datareader.read_data(code/date)` | 总表命中行 |
| wechat | `datareader.read_wechat(date/keyword)` | 舆情 docx 段落 |
| tasks | `tasks.read_log` / `read_meta` | 任务状态 / 失败原因 |
| prompt | 读 `prompt.md` 规则段 | 摘要口径依据 |

### 7.5 Agentic 决策循环
- **首版（规则规划）**：关键词命中源——"风险/担保/换股"→ anno+data；"上次/之前/你说过"→ memory；"失败/任务"→ tasks；"口径"→ prompt；"舆情/公众号"→ wechat。
- **进阶（LLM 规划）**：把「可用源列表 + 用户问题」给 SenseNova，输出 JSON `{sources:[...], queries:{...}}` 再执行检索；评估结果充足性，不足则多跳重试（上限 2 轮）。
- **决策透明**：前端展示「已检索：历史对话 / Anno 表 / 任务日志」，让用户知道依据从哪来。

### 7.6 实现要点与降级
- **零依赖**：记忆检索用标准库（json / re），不引入向量库；语义检索为二期，可选接 SenseNova embedding。
- **截断**：每源片段 ≤ N 字符，总 context 受 SenseNova 上下文约束。
- **隐私**：记忆仅本地文件，不上传；清理走回收站式归档。
- **降级**：检索全失败时退 Mode A（仅会话内上下文 + 规则作答），不报错。

---

## 八、意图识别与确认机制

- **识别**：先做规则关键词分类（成本低、可控）；后续可选 LLM 二分类兜底歧义句。
- **确认**：Action 执行前先回「将要执行：生成整理表，参数 dir=Anno、out=Anno/out」预览卡 + 确认按钮，防误触覆盖文件。设置里可开「免确认模式」。

---

## 九、SenseNova 接入（复用已分析结论，不新写 HTTP）

- `assistant.ask_llm()` 调 `llm.chat(..., override={"backend":"api", "base_url":"https://token.sensenova.cn/v1", "api_key":环境变量 SENSENOVA_API_KEY, "model":"sensenova-6.8-flash-lite"})`。
- `llm.py` 现成的 Bearer 头 / SSE 解析 / 错误分类（401/403/429/404/5xx/timeout）**原样复用**，只需加 `override` 透传参数（见十）。
- 精读仍走全局 `backend=workbuddy` 桥接，**不受影响**；助手问答单独直连 SenseNova。
- 无 key 时自动降级 Q&A 为 Mode A（本地上下文作答），不报错、不卡死。

---

## 十、文件改动清单（纯新增 / 小改，零破坏性）

| 文件 | 改动 | 说明 |
|---|---|---|
| `web/index.html` | 新增 | 「对话助手」首屏 tab + 对话容器 + 快捷指令芯片（默认 active）；历史会话入口 |
| `web/styles.css` | 新增 | `.assistant` 首页布局、`.chat-msg(.user/.bot)`、`.action-card`、`.chip`、`.rag-src` 徽标 |
| `web/app.js` | 新增 | `initAssistant()`：发送 `POST /api/assistant`、渲染消息/动作卡、订阅任务进度（复用 `/api/tasks/<tid>/stream` SSE）、Mode 徽标、会话切换 |
| `app/web/routes.py` | 新增路由 | `h_assistant` → `POST /api/assistant`；可选 `GET /api/assistant/context`、`GET /api/assistant/sessions` |
| `app/core/assistant.py` | 新建 | `classify(msg)` / `answer(msg, ctx)`（Q&A，走 SenseNova）/ `execute_action(name, params)`（Action）/ `rag_answer(msg, ctx)` |
| `app/core/datareader.py` | 新建 | `read_anno()` / `read_data()` / `read_wechat()` / 索引构建 |
| `app/core/memory.py` | 新建 | 会话记忆读写、跨会话检索 `search_memory()` |
| `app/core/retrieval.py` | 新建 | `RetrievalPlanner` 多源检索 + 决策循环 |
| `app/core/config.py` | 小增 | 新增 `assistant` 段（enabled/model/temperature/timeout/max_tokens/env_key）+ `datareader` 段（索引开关/路径）+ `memory` 段（开关/保留天数） |
| `app/core/llm.py` | 小改 | `chat()` / `chat_stream()` 增加 `override` 参数透传（backend/base_url/api_key/model） |

---

## 十一、风险与降级

- **bridge 动作需人工回填**：助手明确提示「请在桥接面板提交」，不假装全自动。
- **免费额度 429**：`_classify` 已把 429 提示限流，前端建议「隔 5h 刷新或换 key」；Q&A 降级本地。
- **数据超长**：datareader 索引定位 + 截断，只带命中行。
- **误操作**：Action 默认确认制；`build` 等覆盖类动作在确认卡明示目标路径。
- **检索失败**：RAG 降级会话内上下文 + 规则作答。

---

## 十二、验收标准

1. 平台首屏即对话助手；说「132xxx.SH 风险如何」，助手从 `Anno/out` 取行 + SenseNova 归纳，附引用。
2. 说「帮我一键精读」，生成 digest 任务 / bridge 待办并提示桥接；说「重建数据」，全自动跑 `/api/data/rebuild` 并直播进度。
3. 说「上次那只债后来怎样了」，助手检索**历史对话记忆** + 当前数据，跨会话回答。
4. 无 `SENSENOVA_API_KEY` 时 Q&A 降级本地、不报错。
5. 公告 / 数据 / 舆情三页与现有按钮、桥接、任务系统**零回归**。

---

## 十三、落地量评估

比「从零造助手」省约 70%——LLM 通道、任务系统、SSE、三个项目的全部功能 API 都已现成。本方案只新增四块胶水代码：**对话首屏 + 意图分类 + 数据读取器 + Agentic RAG（记忆/检索/决策）**。

**建议分期**：
- 阶段一：首屏 + 数据读取器（只读成品表档）+ 全自动动作 + SenseNova 直连。
- 阶段二：Agentic RAG（memory + retrieval + 规则规划）+ 桥接动作提示。
- 阶段三：LLM 规划多跳 + 语义检索（可选 embedding）+ 会话持久化 UI。
